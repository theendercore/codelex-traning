interface Configuration {
  maxLevel: number;
  defaultSpeed: number;
  nbCellsX: number;
  nbCellsY: number;
}

const configuration: Configuration = {
  maxLevel: 2,
  defaultSpeed: 200,
  nbCellsX: 45,
  nbCellsY: 25,
};

export default configuration;
